/** 
 * A module that represents the operating systems supported by the application
 * @module SupportedOS
 * 
 */

/**
 * Operating systems supported
 * @enum
 */
const SupportedOS = {
	LINUX: 'Linux',
	WIN32: 'Windows',
	DARWIN: 'macOS'
}

module.exports = SupportedOS;